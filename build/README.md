# Build resources

`icon.svg` is an original Frostline Studio asset created for this repository. It may be redistributed under the repository's MIT License.

`electron-builder` converts the SVG into the Windows executable and installer icons during packaging. Generated installers remain under the ignored `release/` directory until a maintainer deliberately publishes them.
